At every changing style of the element, you need to do this in special element's css file.
At second, you need to check bugs for every element variation inside special element's html file.
Do this in every browser starting from IE8.
Dynamic classnames sets on to parent div (as example .codename-pricing-table.d-table).